# Narendra Rane Portfolio (Next.js)

This is a Next.js portfolio website generated from resume content.

## Run locally

```bash
npm install
npm run dev
```

Open `http://localhost:3000`.

## Build

```bash
npm run build
npm run start
```

## Deploy

### Vercel

1. Push this folder to GitHub.
2. Import the repo in Vercel.
3. Framework preset: Next.js (auto-detected).
4. Deploy.

### Netlify

1. Push this folder to GitHub.
2. Import the repo in Netlify.
3. Build command: `npm run build`
4. Publish directory: `.next`
5. Ensure `@netlify/plugin-nextjs` is installed during build (configured in `netlify.toml`).

## Resume download

The resume file is served at:

`/Narendra_Rane_Resume_2.pdf`
